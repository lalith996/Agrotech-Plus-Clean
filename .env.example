# Database (Required)
# For local development, use a local PostgreSQL database
# For production (Vercel), use Neon serverless PostgreSQL
DATABASE_URL="postgresql://username:password@localhost:5432/agrotrack?schema=public"

# NextAuth.js Authentication (Required)
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Optional - for social login)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ========================================
# EXTERNAL APIs REMOVED IN CLEAN VERSION
# ========================================
# The following external services have been removed:
# - SendGrid (email) - replaced with console logging
# - Twilio (SMS) - replaced with console logging
# - Stripe (payments) - simplified or removed
# - AWS S3 (file storage) - removed
# - Google Maps - removed
# - Firebase - removed
#
# All email/SMS notifications are now logged to console.
# Check server logs to see what would have been sent.

# Gemini AI (Optional - for chatbot)
GEMINI_API_KEY="your-gemini-api-key"
GEMINI_MODEL="models/gemini-1.5-flash"

# Redis Configuration (Optional - for caching)
REDIS_URL="redis://localhost:6379"

# ML Service Configuration (Optional - for AI/ML features)
ML_SERVICE_URL="http://localhost:8000"
ML_SERVICE_API_KEY="dev-api-key-change-in-production"
ML_SERVICE_TIMEOUT="5000"

# Environment
NODE_ENV="development"

# Security (Production only)
# HSTS_MAX_AGE="31536000"
# FORCE_HTTPS="true"